# =============================================================================
# DOCKER KF2 CONTAINER CONFIGURATION
# =============================================================================
# IMPORTANT: This file contains sensitive credentials.
# - Copy this file to .env: cp example.env .env
# - Set restrictive permissions: chmod 600 .env
# - NEVER upload the .env file to Git (must be in .gitignore)

# -----------------------------------------------------------------------------
# AUTHENTICATION AND ACCESS
# -----------------------------------------------------------------------------

# Password for the linuxgsm user inside the container
# REQUIRED: Define a secure password for SSH access and sudo
# Default: (blank)
LGSM_PASSWORD=

# Custom SSH port to access the container
# Recommended: Use a different port than 22 for security (e.g.: 2222)
# Default: 22 (TCP)
SSH_PORT=

# SSH public keys for passwordless authentication
# Format: ssh-rsa AAAAB... or separate multiple keys with commas
# Optional: If not defined, only password access will be available
# Default: (blank)
SSH_KEY=

# -----------------------------------------------------------------------------
# KILLING FLOOR 2 SERVER PORTS
# -----------------------------------------------------------------------------

# Main game port - Players connect to this port
# Default: 7777 (UDP)
KF2_GAME_PORT=

# Steam query port - Used for server browser and Steam Master Server communication
# Default: 27015 (UDP)
KF2_QUERY_PORT=

# Web Admin port - Access to the web administration panel
# Default: 8080 (TCP)
KF2_WEBADMIN_PORT=

# Steam networking port - Used by Steam for server communication
# Default: 20560 (UDP)
KF2_STEAM_PORT=

# NTP port - Only needed for Weekly Outbreak mode (time synchronization)
# Default: 123 (UDP)
KF2_NTP_PORT=

# -----------------------------------------------------------------------------
# KILLING FLOOR 2 ADMIN CONFIGURATION
# -----------------------------------------------------------------------------

# Web Admin enabled - Enable/disable the web administration panel
# Default: false (true/false)
KF2_WEBADMIN=

# Admin password for the KF2 WebAdmin (leave blank for no admin password)
# Default: (blank)
KF2_ADMIN_PASSWORD=

# Multi admin support for WebAdmin (true/false)
# Default: false
KF2_MULTI_ADMIN=

# -----------------------------------------------------------------------------
# KILLING FLOOR 2 Server CONFIGURATION
# -----------------------------------------------------------------------------

# Workshop items to subscribe (comma separated, no spaces, e.g. [2592435250,3480853434])
# Default: (blank)
KF2_WORKSHOP=[]

# Tickrate del servidor (NetServerMaxTickRate en [IpDrv.TcpNetDriver])
# Default: 30
KF2_TICKRATE=